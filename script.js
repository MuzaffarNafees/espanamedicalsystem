const themeLink=document.createElement('link');themeLink.rel='stylesheet';themeLink.href='theme.css';document.head.appendChild(themeLink);
document.querySelectorAll('.brand span:last-child').forEach((brand)=>{brand.textContent='Espana Medical System'});
document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('#enquiry-form');if(!form)return;form.action='https://formsubmit.co/ajax/majidchaudhary123@gmail.com';form.method='POST';form.noValidate=true;form.addEventListener('submit',async(event)=>{event.preventDefault();event.stopImmediatePropagation();const status=form.querySelector('.form-status');const name=form.elements.name.value.trim();const email=form.elements.email.value.trim();const message=form.elements.message.value.trim();const service=form.elements.service.value;const errors=[];if(name.length<2)errors.push('Please enter your full name.');if(!/^\S+@\S+\.\S+$/.test(email))errors.push('Please enter a valid work email.');if(!service)errors.push('Please select a service.');if(message.length<20)errors.push('Please add at least 20 characters about your requirement.');if(errors.length){status.textContent=errors.join(' ');status.className='form-status is-error';return}status.textContent='Sending your enquiry...';status.className='form-status is-sending';try{const response=await fetch(form.action,{method:'POST',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify({name,email,organisation:form.elements.organisation.value.trim(),service,message,_subject:'New Espana Medical System enquiry',_captcha:'false'})});if(!response.ok)throw new Error('Request failed');form.reset();status.textContent='Thank you. Your enquiry has been sent to Espana Medical System.';status.className='form-status is-success'}catch(error){status.textContent='We could not send your enquiry right now. Please try again.';status.className='form-status is-error'}},{capture:true})});
document.addEventListener('DOMContentLoaded',()=>{const p=document.querySelector('.scroll-progress'),t=document.querySelector('.nav-toggle'),m=document.querySelector('.nav-menu');const update=()=>{const h=document.documentElement.scrollHeight-innerHeight;if(p)p.style.width=`${h?(scrollY/h)*100:0}%`};update();addEventListener('scroll',update,{passive:true});t?.addEventListener('click',()=>{const open=m.classList.toggle('is-open');t.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.nav-menu a').forEach(a=>a.addEventListener('click',()=>{m?.classList.remove('is-open');t?.setAttribute('aria-expanded','false')}));document.addEventListener('click',e=>{if(m&&t&&!m.contains(e.target)&&!t.contains(e.target)){m.classList.remove('is-open');t.setAttribute('aria-expanded','false')}});const items=document.querySelectorAll('.reveal');if('IntersectionObserver'in window){const o=new IntersectionObserver((entries,obs)=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');obs.unobserve(e.target)}}),{threshold:.12});items.forEach(i=>o.observe(i))}else items.forEach(i=>i.classList.add('is-visible'))});